/**
 * Tenant registry.
 * Add a new string entry for each clinic client you onboard.
 * The special value 'default' maps to the original, un-prefixed collection
 * names so existing data requires no migration.
 */
export const tenants = ['firstClinic'];
